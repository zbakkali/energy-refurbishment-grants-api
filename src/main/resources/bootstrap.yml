spring:
  application.name: energy-refurbishment-grants-api
  cloud.config.uri: ${SPRING_CONFIG_URI:http://localhost:8888}
eureka:
  instance:
        statusPageUrlPath: /actuator/info
        healthCheckUrlPath: /actuator/health
  client.serviceUrl.defaultZone: http://localhost:8761/eureka/
